[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && playwright install chromium --with-deps && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
